const Footer = () => {
  return (
    <footer className=" w-full bg-gray-800 text-white text-center py-4 mt-12">
      <p>&copy; {new Date().getFullYear()} AI Agents Studio. Всі права захищені.</p>
    </footer>
  );
};

export default Footer;